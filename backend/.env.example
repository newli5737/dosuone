# PostgreSQL local (chỉnh theo máy bạn)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_postgres_password
DB_NAME=dosuone

JWT_SECRET=your_jwt_secret_here_change_in_production
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

PORT=3000

# Production VPS (PM2): PORT=3018, xem deploy/env.backend.example
# CORS_ORIGINS=http://one.dosutech.site,https://one.dosutech.site
