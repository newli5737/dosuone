# Copy vào /home/dosuTech/appbandt/backend/.env
# PostgreSQL — giống dev (local trên VPS)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=test1234
DB_NAME=dosuone

JWT_SECRET=8e225409175b5a9305373c030c1b26fcb71fca92ea64cda23b23d18b946386d5
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Port riêng (không dùng 3000 — tránh trùng PM2)
PORT=3018

# Admin FE (HTTP trước certbot; thêm https sau certbot)
CORS_ORIGINS=http://one.dosutech.site,https://one.dosutech.site
